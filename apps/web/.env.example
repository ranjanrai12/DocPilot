# --- apps/web ---
# Copy to .env. In dev, API calls go through the Vite proxy (see vite.config.ts),
# so VITE_API_BASE_URL is only needed for production builds.

# VITE_API_BASE_URL=http://localhost:3000
# VITE_SENTRY_DSN=
