{
  "name": "@docpilot/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(shared) no lint configured yet\"",
    "build": "echo \"(shared) source-only package, nothing to build\"",
    "test": "echo \"(shared) no tests yet\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
